"use client";

import { NavBar } from "./Nav";

export const Header = () => {
  return (
    <header className="font-sans w-full flex justify-center items-center h-28">
      <NavBar />
    </header>
  );
};
