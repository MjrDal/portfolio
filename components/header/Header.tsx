"use client";

import { NavBar } from "./Nav";

export const Header = () => {
  return (
    <header className=" w-full flex justify-center items-center h-28 bg-black">
      <NavBar />
    </header>
  );
};
